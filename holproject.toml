[holbuild]
schema = 1

[project]
name = "vyper-hol"
version = "0.1.0"

[build]
members = ["."]
roots = ["vyperHolScript.sml"]
exclude = ["tests/generated/*", "test_*.sml", "semantics/prop/test_*.sml"]

[dependencies.verifereum]
manifest = "verifereum.manifest.toml"

# LOG: Type Soundness Repair

L001: [AugAssign_cont] FAILED[structure]: `>>` after Cases_on applies tactics to both INL/INR subgoals — need `>- suspend / suspend` pattern. Session 73.
L002: [AugAssign_cont] FAILED[tactics]: Even after structural fix to use `>- suspend`, Resume blocks with real proofs fail. Root cause: `AugAssign_cont` must contain the suspend calls (not be cheated). Session 73.
L003: [KEY][AugAssign_atwt] FAILED[irule_at]: `rpt (first_assum (irule_at Any))` creates existential goals for `value_has_type (leaf_type tv (REVERSE sbs)) nv` that can't be resolved. Need explicit `qexistsl` witnesses. Session 73.
L004: [KEY][close_inr_err_tac] INSIGHT: This tactic ALREADY handles no-TypeError (via `TRY not_type_error_tac`). Many `>>`-after-Cases_on patterns in working blocks use `TRY close_inr_err_tac` which succeeds on INR and fails harmlessly on INL. This is a VALID pattern when close_inr_err_tac applies. Session 73.
L005: [AugAssign_cont] BATCH APPROACH NEEDED: 27+ blocks failing, can't debug one-at-a-time. Need to: (1) restore AugAssign_cont with real tactics + suspends, (2) cheat problematic Resume blocks, (3) fix others systematically. Session 73.
L006: [AugAssign_cont] PROVED (structure): Replaced cheat with real tactic using guarded IH specialization + Cases_on get_Value + suspend calls for 4 sub-blocks (gv_inr, atwt, at_inl, at_inr). Session 75.
L007: [KEY][AugAssign_cont] FAILED[syntax]: `>> gvs[AllCaseEqs()] >-` causes type error (precedence). Must parenthesize: `Cases_on 'q' >- (..INL..) >> (..INR..)`. Session 75.
L008: [AugAssign_cont] FAILED[IH-args]: Used `qspecl_then ['env','r','INL x','r']` for eval_expr IH, but input state is `st_bt` not `r`. Fix: `['env','st_bt','INL x','r']`. Session 75.
L009: [KEY][guarded-IH] INSIGHT: eval_expr IH is guarded: `∀tv. INL x = INL tv ⇒ ...toplevel_value_typed tv tyv`. This means `not_type_error_tac` can't find `toplevel_value_typed x tyv` directly. Must instantiate IH first: `qpat_x_assum '∀tv. INL x = INL tv ⇒ _' (qspecl_then ['x'] mp_tac) >> simp[] >> strip_tac`. Session 75.
L010: [AugAssign_gv_inr] FAILED[hallucination]: Used `well_typed_binop_same_type` which does NOT exist. Must either find equivalent or prove it. Session 75.
L011: [AugAssign_gv_inr] APPROACH: After gvs[AllCaseEqs()] + guarded IH instantiation, need to show tyv ≠ NoneTV and tyv ≠ ArrayTV t b. well_typed_binop forces t2=ty for most ops, and ty is constrained (numeric/int/bool/flag). Use evaluate_type_not_NoneT_imp_not_NoneTV + evaluate_type_BaseT_imp_not_ArrayTV. Session 75.
L012: [KEY][eval_preserves_swt] THEOREM FALSE: well_typed_binop ty In ty (expr_type e) allows expr_type e = ArrayT, producing ArrayRef. get_Value ArrayRef = TypeError. Counterexample EVAL-verified. Session 76.
L013: [eval_preserves_swt] FIX APPLIED: Added `bop ≠ In ∧ bop ≠ NotIn` to well_typed_stmt AugAssign definition. NOT YET BUILT. Session 76.
L014: [AugAssign_gv_inr]Still references nonexistent well_typed_binop_same_type. After definition fix, should use bop≠In/NotIn + Cases_on bop + toplevel_value_typed_for_BaseT_expr_type. Session 76.
L015: [evaluate_type_FlagT_imp_not_ArrayTV] PROVED: FlagT variant of BaseT lemma. Needed for AugAssign And/Or/XOr cases where tyv could be FlagTV. Session 78.
L016: [not_NoneT_not_ArrayT] PROVED[simp]: Boundary lemma — type classification predicates (is_int_type, is_numeric_type, is_bool_type, is_flag_type, is_comparable_type) all imply type ≠ NoneT ∧ type ≠ ArrayT. Session 78.
L017: [well_typed_binop_not_In_second_type] PROVED: Boundary lemma for non-In/NotIn binops. well_typed_binop ty bop ty t2 + bop≠In/NotIn ⟹ t2≠NoneT ∧ t2≠ArrayT. Session 78.
L018: [KEY][simp + disjunctions] INSIGHT: `simp[not_NoneT_not_ArrayT]` on disjunctive goals like `is_int_type ty ∨ is_bool_type ty ∨ is_flag_type ty ⇒ ...` does NOT work. simp doesn't break disjunctions. Must use `strip_tac >> gvs[]` first. Session 78.
L019: [AugAssign_gv_inr] PROVED: Fixed by replacing well_typed_binop_same_type (nonexistent) with well_typed_binop_not_In_second_type + Cases_on expr_type + evaluate_type lemmas. Session 78.
L020: [AugAssign_at_inr] PARTIALLY FIXED: Added explicit accounts_well_typed handling via metis_tac[cj 1 assign_target_preserves_accounts]. But build still fails with Q_TAC0/FIRST_ASSUM error in some other block. Session 78.
L021: [build] FAIL: Q_TAC0/FIRST_ASSUM error after AugAssign sub-blocks. Some block uses a quotation pattern (qpat_x_assum) that doesn't match new conclusion structure. Need hol_state_at to pinpoint. Session 78.
L022: [AugAssign_gv_inr] PROVED: Fixed dead qpat_x_assum pattern. gvs[AllCaseEqs()] already resolved guarded IH `∀tv. INL x = INL tv ⇒ ...`, so the pattern no longer existed. Changed to use `tyv` directly from gvs output. Session 82.
L023: [AugAssign_at_inr] PROVED: gvs[pair_case_thm, return_def] auto-discharged state_well_typed/env_consistent/accounts_well_typed conjuncts (st'=r'' matched assumptions). Rewrote from positional ACCEPT_TAC to explicit CONJ_TAC >- drule. Session 82.
L024: [KEY][gvs-transforms-goals] INSIGHT: gvs[AllCaseEqs()] resolves guarded IHs (instantiating ∀tv. INL x = INL tv ⇒ ...). gvs[pair_case_thm, return_def] auto-discharges conjuncts matching assumptions after VAR_EQ_TAC. Both change goal shapes, breaking positional/ACCEPT_TAC-dependent tactics. Session 82.
L025: [If] FAILED[FIRST_ASSUM]: `first_assum ACCEPT_TAC` after `irule push_scope_swt` likely fails because scope_bracket_preserves doesn't include accounts_well_typed. Push/pop_scope only modify st.scopes, so accounts are preserved trivially. Need companion lemma. Session 82.
L026: [KEY][close_inr_err_tac] INSIGHT: After `drule_all >> strip_tac`, the IH conclusion IS the goal (all conjuncts). `no_return_from_eval` adds facts. `rpt (CONJ_TAC >> TRY (first_assum ACCEPT_TAC))` tries each piece but `accounts_well_typed st'.accounts` may not match any assumption because the IH conclusion is the goal, not in assumptions. Need explicit handling. Session 82.
L027: [build] Build now takes ~728s due to all Resume block replaying. Interactive hol_state_at at deep blocks times out. Session 82.
L028: [KEY][close_inr_err_tac] FAILED 5 APPROACHES: (1) FIRST_ASSUM+ORELSE NO_TAC — too slow, drule_all on non-matching IHs expensive; (2) PRED_ASSUM with push_scope exclusion — still matches wrong IH; (3) apply_eval_ih inside FIRST — qpat_x_assum consumes IH on failure, cascade; (4) Smarter PRED_ASSUM — can't exclude all guarded patterns; (5) apply_eval_ih as replacement — same qpat_x_assum consumption hazard. Session 84.
L029: [KEY][qpat_x_assum] CRITICAL HAZARD: qpat_x_assum REMOVES the assumption BEFORE applying the continuation. If drule_all fails inside FIRST/[...], the assumption is permanently lost. This makes qpat_x_assum+drule_all inside FIRST chains UNSAFE. Use qpat_assum (keeps) in FIRST chains, or explicit patterns with >-. Session 84.
L030: [KEY][guarded-IH-blocks] Only If, IfExp, For blocks have guarded IHs (from eval_expr→push_scope→eval_stmts composition). All other blocks have standard unguarded IHs where PRED_ASSUM+drule_all works fine. DON'T modify close_inr_err_tac — handle these blocks explicitly. Session 84.
L031: [If block] FAILED[FIRST_ASSUM/NO_TAC]: `first_x_assum drule_all` in If parent block picks up guarded P1 IH (has eval_expr+push_scope in antecedent), drule_all can't resolve push_scope, FIRST_ASSUM fails. Fix: use `apply_eval_ih \`INL x\` \`r\`` with targeted qpat_x_assum, or `>- close_inr_err_tac` to isolate INR case. Session 84.
L032: [scope_bracket_preserves] EXTENDED: Added accounts_well_typed st_body.accounts as hypothesis and accounts_well_typed (...).accounts as conclusion. Proved since scopes don't affect accounts. Session 84.
L033: [If_True/If_False] ADDED accounts_well_typed for pushed state via `simp[evaluation_state_component_equality]`. Session 84.

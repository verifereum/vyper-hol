# LOG: Type Soundness Repair

L037: [Assign_tgt] Identified guarded-IH root cause: PRED_ASSUM can't find well_typed because IH is nested under eval_target guard → FIX: apply guarded IH before case split → eliminated PRED_ASSUM error but exposed second bug: close_inr_err_tac can't close P0 return-typing conjunct → tried 4 tactical variations, all failed → RETROSPECTIVE: the working AugAssign proof (lines 1337-1343) has the exact solution: `imp_res_tac eval_expr_not_return` resolves the return-typing conjunct → next session: copy AugAssign pattern directly
L038: [KEY][Assign_tgt] INR error propagation for P0 goals REQUIRES eval_expr_not_return in addition to IH. close_inr_err_tac doesn't handle P0 return-typing conjunct. AugAssign lines 1337-1343 has the exact working pattern. Also: `>-` with `suspend` doesn't work (suspend doesn't solve inline). Also: `>> TRY` for case splits is wrong (applies to ALL subgoals, not one).
L039: [Assign_tgt] 5 fix attempts all failed. Root causes: (1) `>-` inside Resume → gentactic type error, (2) `qpat_x_assum` with `_` wildcards → Q_TAC0, (3) TRY silently fails on wrong subgoal (INL vs INR after reverse), (4) `suspend` for INR leaves unexpected goal state for `>-`. Decomposition fix: use `suspend` for BOTH branches of eval_expr case split, avoid `qpat_x_assum` with `_`, use `first_x_assum drule_all` or PRED_ASSUM in suspended child blocks. KEY INSIGHT: the guarded IH pattern `!s'' gv t. eval_target _ _ s'' = _ ==> _` works with `_` but the unguarded IH pattern `!env st res st'. well_typed_expr _ _ /\ _ ==> _` does NOT.

L040: [Assign_tgt] Tried 8+ iterations to fix Q_TAC0 error in children blocks → root cause is variable naming fragility in `by` blocks and case explosion from nested TRY+NO_TAC. Decomposition is wrong: too many nested case splits. Fix: inline assign_target_well_typed (no by-block), use AugAssign combined finish pattern, rename1 after Cases_on.

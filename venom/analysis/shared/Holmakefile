# Shared analysis infrastructure: memory locations, access helpers
# defs/ = definitions, proofs/ = property proofs
INCLUDES = $(VFMDIR)/util $(VFMDIR)/spec ../.. ../../../syntax ../../../semantics defs proofs
